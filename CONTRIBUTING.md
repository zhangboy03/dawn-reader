# 参与 Dawn Reader

谢谢你愿意一起把英文原著阅读做得更顺手。我们最看重的是：问题来自真实阅读，改动保持小而清楚，数据和版权边界说得明白。

## 开始之前

1. 先搜索现有 Issue；功能想法请说明具体阅读场景，而不只是功能名称。
2. 较大的交互、同步协议或数据结构改动，请先开 Discussion 或 Issue 对齐方案。
3. 不要上传 EPUB、视频文字稿、API 密钥、访问令牌或包含私人阅读记录的数据库。

## 开发流程

```bash
npm install
npm test
npm run build
npm audit --omit=dev
```

原生 iPad 改动还应在 Xcode 中运行 `DawnReaderTests`，并在真机或 iPad 模拟器验证导入、阅读、同步和删除路径。

提交 PR 时请包含：

- 用户能观察到的变化；
- 为什么这符合“原文优先，卡住才帮”；
- 自动化测试，以及不能自动化验证的手工步骤；
- 涉及 UI 时的 iPad 与窄屏截图；
- 涉及同步时的失败与重试行为。

## 代码约定

- TypeScript 保持严格类型，不用 `any` 绕过边界；
- 副作用要有失败状态，尤其是上传、删除、同步和 AI 请求；
- 新文案用自然、具体的中文，不制造学习焦虑；
- 新依赖需要说明必要性、维护状态和许可证；
- 提交信息用简短祈使句，例如 `Add remote-first book deletion`。

参与本项目即表示你同意遵守 [行为准则](CODE_OF_CONDUCT.md)，并同意你的贡献按 [Apache-2.0](LICENSE) 许可发布。
