# 安全政策

## 支持范围

安全修复目前只应用于默认分支上的最新版本。Dawn Reader 仍处于 v0.x 阶段，不承诺旧提交的安全支持。

## 私下报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下报告，不要为未修复的漏洞创建公开 Issue。报告中请提供影响、复现步骤、受影响版本和可行的缓解建议；不要包含真实 API 密钥、完整 EPUB 或他人的阅读数据。

维护者会尽量在 7 天内确认收到，并在验证影响后协调披露时间。此时间是响应目标，不是修复保证。

## 部署者责任

- 通过部署平台的 Secret 管理 AI 密钥，不写入源码或客户端；
- 为 D1、R2 和身份认证使用最小权限，并定期轮换凭据；
- 公开服务必须保留用户隔离、上传大小限制和 HTTPS；
- 自托管者应自行维护依赖更新、备份与数据删除流程。
